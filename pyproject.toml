[project]
name = "btc-historical-data-research"
version = "0.1.0"
description = "Step-by-step research tasks using historical BTC trading data."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
